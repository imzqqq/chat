server {
    listen 443 ssl; # IPv4
    listen [::]:443 ssl; # IPv6
    server_name my.hostname.com;

    ssl_certificate /path/to/fullchain.pem;
    ssl_certificate_key /path/to/privkey.pem;
    ssl_dhparam /path/to/ssl-dhparams.pem;

    proxy_set_header Host      $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_read_timeout         600;

    location /.well-known/matrix/server {
        return 200 '{ "m.server": "my.hostname.com:443" }';
    }

    location /.well-known/matrix/client {
        # If your sever_name here doesn't match your matrix homeserver URL
        # (e.g. hostname.com as server_name and matrix.hostname.com as homeserver URL)
        # add_header Access-Control-Allow-Origin '*';
        return 200 '{ "m.homeserver": { "base_url": "https://my.hostname.com" } }';
    }

    # route requests to:
    # /chat/client/.*/sync
    # /chat/client/.*/user/{userId}/filter
    # /chat/client/.*/user/{userId}/filter/{filterID}
    # /chat/client/.*/keys/changes
    # /chat/client/.*/rooms/{roomId}/messages
    # to sync_api
    location ~ /chat/client/.*?/(sync|user/.*?/filter/?.*|keys/changes|rooms/.*?/messages)$  {
        proxy_pass http://sync_api:8073;
    }

    location /chat/client {
        proxy_pass http://client_api:8071;
    }

    location /chat/federation {
        proxy_pass http://federation_api:8072;
    }

    location /chat/key {
        proxy_pass http://federation_api:8072;
    }

    location /chat/media {
        proxy_pass http://media_api:8074;
    }
}
