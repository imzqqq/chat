import Foundation

enum CustomSchemeURLConstants {
    
    enum Parameters {
        static let transactionId = "transaction_id"
    }
    
    enum Hosts {
        static let connect = "connect"
    }
}
