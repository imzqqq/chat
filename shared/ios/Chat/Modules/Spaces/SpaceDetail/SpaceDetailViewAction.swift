import Foundation

/// `SpaceDetailViewController` view actions exposed to view model
enum SpaceDetailViewAction {
    case loadData
    case join
    case open
    case leave
    case dismiss
    case dismissed
}
